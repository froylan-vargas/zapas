# Local build benefits
- It is important to have a local clean build in local before trying hosted cloud envs.
- Helps to review vulnerable or oudated libs, check env variables that should be added to envs.  
- Check if changes added stateful dependencies that should be considered in cloud envs. 